# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual env
venv/
.venv/
env/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Word lockfiles
~$*

# Local secrets / env
.env
.env.local
*.pem
*.key

# Raw partner data (too large / redistribution unclear) — ignore the whole folder,
# including downloaded PDFs and the GeoPackage. Regenerate locally from source.
data/raw/

# Docker volumes (if bind-mounted)
mappa-db-data/

# Ingestion / eval outputs
data/generated/
data/inventory/
logs/
.mappa_db_pw

# Extracted document text produced by pipelines/fetch_parse_local.py (derived, large)
data/corpus_*.json

# La Maraña's master inventory workbook (partner data, pulled from their Drive)
data/raw/lamarana_master_inventory.xlsx
